apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: banking

spec:
  replicas: 1

  selector:
    matchLabels:
      app: frontend

  template:
    metadata:
      labels:
        app: frontend

    spec:
      nodeSelector:
        kubernetes.io/hostname: work-node-1
      containers:
        - name: frontend
          image: divaypratap/java-frontend:v1
          imagePullPolicy: Always
          ports:
            - containerPort: 80

          